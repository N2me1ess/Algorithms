#ifndef SORTHEADER_H
#define SORTHEADER_H

#include <vector>

// applies insertion sort on vector
void insertionSort(std::vector<int>& v);

void insertionSortReverse(std::vector<int>& v);

int searchVector(std::vector<int>& A, int v);
#endif